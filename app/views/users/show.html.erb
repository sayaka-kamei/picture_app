<h1><%= @user.name %> Page</h1>

<table>
  <tr>
    <th>Mail address:</th>
    <th><strong>Image:</strong></th>
  </th>  


  <tr>
    <td><%= @user.email %></td>
    <td><%= image_tag @user.user_image.url %></td>
    <% if @user.id == current_user.id %>
      <td><%= link_to 'プロフィールを編集する', edit_user_path %></td>
    <% end %>  
  </tr>
 
</table>    

<%= link_to "Pictures list", pictures_path, class:"btn" %>